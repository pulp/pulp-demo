#version=DEVEL
ignoredisk --only-use=sda
# Partition clearing information
clearpart --none --initlabel
# Use graphical install
graphical
# Use hard drive installation media
harddrive --dir=None --partition=/dev/mapper/live-base
# Keyboard layouts
keyboard --vckeymap=us --xlayouts='us'
# System language
lang en_US.UTF-8

# Network information
network  --hostname=localhost.localdomain
#Root password
rootpw --lock
# X Window System configuration information
xconfig  --startxonboot
# Run the Setup Agent on first boot
firstboot --enable
# System services
services --enabled="chronyd"
# System timezone
timezone America/New_York --isUtc
# Disk partitioning information
part /boot --fstype="xfs" --ondisk=sda --size=1024
part pv.702 --fstype="lvmpv" --ondisk=sda --size=698376 --grow
part /boot/efi --fstype="efi" --ondisk=sda --size=200 --fsoptions="umask=0077,shortname=winnt"
volgroup fedora_localhost-live --pesize=4096 pv.702
logvol swap --fstype="swap" --size=32768 --name=swap --vgname=fedora_localhost-live
logvol /home --fstype="xfs" --size=51200 --name=home --vgname=fedora_localhost-live
logvol / --fstype="xfs" --grow --size=1024 --name=root --vgname=fedora_localhost-live
logvol /var/lib/libvirt --fstype="xfs" --size=512000 --name=var_lib_libvirt --vgname=fedora_localhost-live
logvol /var --fstype="xfs" --size=51200 --name=var --vgname=fedora_localhost-live

%addon com_redhat_kdump --disable --reserve-mb='128'

%end

%anaconda
pwpolicy root --minlen=6 --minquality=1 --notstrict --nochanges --notempty
pwpolicy user --minlen=6 --minquality=1 --notstrict --nochanges --emptyok
pwpolicy luks --minlen=6 --minquality=1 --notstrict --nochanges --notempty
%end
